query getAllMembers($member: ID!) {
  allMembers(
    filter: {
      id_not: $member
    }
  ) {
    id
    name
    title
    image
    chats(
      filter: {
        members_some: {
          id: $member
        }
      }, first: 1
    ) {
      id
    }
  }
}
